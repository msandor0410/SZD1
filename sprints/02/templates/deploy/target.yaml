type: local_preview

build:
  command: "echo 'Angular build helyben történik (ng serve)'"
  artifacts: []

serve:
  command: "echo 'Spring Boot + Angular lokálisan fut'"
  base_url: "http://localhost:8080"

smoke:
  path: "/api/characters"
  expect_status: 200

iac:
  tool: "terraform"
  dir: "infra/terraform"
  plan_out: "infra/terraform/plan.out"
